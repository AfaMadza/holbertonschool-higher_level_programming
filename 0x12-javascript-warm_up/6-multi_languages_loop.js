#!/usr/bin/node

function printArr () {
  let strArr = ['C is fun', 'Python is cool', 'Javascript is amazing'];
  for (let i = 0, size = strArr.length; i < size; i++) {
    console.log(strArr[i]);
  }
}
printArr();
